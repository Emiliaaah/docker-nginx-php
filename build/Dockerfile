# use latest alpine as base
FROM alpine:latest

# copy build script into container
ADD build-nginx-docker.sh /build-nginx-docker.sh

# make build script executable
RUN chmod +x /build-nginx-docker.sh
